[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aiida-open_circuit_voltage"
version = "0.7.0"
description = "The AiiDA plugin to calculate ocv at various charge of states using QE"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT License" }
authors = [{ name = "Tushar Thakur", email = "tushar.thakur@epfl.ch" }]
classifiers = [
    "Framework :: AiiDA",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python",
    "Development Status :: 4 - Beta",
    "Natural Language :: English",
    "Intended Audience :: Science/Research",
]
# DFT+U+V (Hubbard) mode is unsupported until aiida-hubbard releases aiida-quantumespresso 5 support (PR #119); use tag v0.6 for Hubbard runs. When it ships, add an optional extra hubbard = ["aiida-hubbard>=<that version>"].
dependencies = [
    "aiida-core~=2.8",
    "aiida-quantumespresso~=5.0",
    "supercellor>=0.0.1",
    "numpy",
    "importlib_resources",
]

[project.urls]
Homepage = "https://github.com/tsthakur/aiida-open_circuit_voltage"

[project.optional-dependencies]
testing = [
    "pgtest~=1.3.1",
    "wheel~=0.31",
    "coverage",
    "pytest~=6.0",
    "pytest-cov",
]
pre-commit = [
    "pre-commit~=2.2",
    "pylint>=2.5.0",
]
docs = [
    "sphinx",
    "myst-parser",
    "sphinx-design",
    "sphinx-book-theme",
    "sphinxcontrib-details-directive~=0.1.0",
]

[project.entry-points."aiida.workflows"]
"quantumespresso.ocv.ocvwc" = "aiida_open_circuit_voltage.workflows.workchain:OCVWorkChain"

[tool.hatch.build.targets.sdist]
exclude = ["docs/", "tests/"]
